# 목차

- [개요](#개요)
- [step1 완료 상태](#step1-완료-상태)
- [step2 후보](#step2-후보)
  - [플랫폼 홈 랜딩 고도화](#플랫폼-홈-랜딩-고도화)
  - [feed 화면 디테일 개선](#feed-화면-디테일-개선)
  - [서비스별 하단 툴바 정책 정리](#서비스별-하단-툴바-정책-정리)
  - [서비스별 홈 로고와 상단 서비스명 디자인](#서비스별-홈-로고와-상단-서비스명-디자인)
- [추천 step2](#추천-step2)
- [step2 구현 전 분석](#step2-구현-전-분석)
  - [현재 상태](#현재-상태)
  - [문제점](#문제점)
  - [추천 하단 툴바 정책](#추천-하단-툴바-정책)
  - [feed 화면 개선 방향](#feed-화면-개선-방향)
  - [구현 범위](#구현-범위)
  - [이번 작업에서 제외](#이번-작업에서-제외)
  - [추천 구현 순서](#추천-구현-순서)
- [추가사항 분석](#추가사항-분석)
  - [범위 분리](#범위-분리)
  - [하단 툴바 정책 분석](#하단-툴바-정책-분석)
  - [네이밍 제안](#네이밍-제안)
  - [상단 검색 UX 분석](#상단-검색-ux-분석)
  - [상단 알림 UX 분석](#상단-알림-ux-분석)
  - [플랫폼 로그인 메인 카드화](#플랫폼-로그인-메인-카드화)
  - [추가사항 추천 구현 순서](#추가사항-추천-구현-순서)
  - [추가사항 구현 제외 범위](#추가사항-구현-제외-범위)
  - [추가사항 결론](#추가사항-결론)

---

# 개요

- `plan1.md`의 `내 계획 반영 후 재분석` 기준으로 feed 서비스 1차 구조를 구현했다.
- 이제 다음 단계에서는 feed 화면을 더 서비스답게 만들고, 서비스별 하단 툴바 정책을 정리하는 것이 좋다.

---

# step1 완료 상태

완료된 작업:

- `/feed` 라우트 추가
- `features/feed` 구조 추가
- feed guest/member 화면 뼈대 추가
- 서비스 런처에서 `blog`, `feed` 활성화
- `memo`, `schedule`, `account-book` 준비 중 disabled 처리
- 서비스 순서 정리
  - 블로그
  - 피드
  - 메모
  - 일정
  - 가계부
- 일정 아이콘을 달력 아이콘으로 변경

---

# step2 후보

## 플랫폼 홈 랜딩 고도화

현재 플랫폼 홈 비로그인 랜딩은 blog/feed 중심 안내까지 반영되었지만, 아직 카드형 설명에 가깝다.

개선 후보:

- `blog`, `feed`를 핵심 서비스로 더 잘 보여주는 랜딩 섹션 구성
- 준비 중 서비스는 별도 coming soon 느낌으로 정리
- 활성 서비스와 미래 서비스의 차이를 더 명확하게 표시

## feed 화면 디테일 개선

현재 feed 화면은 구조 뼈대 수준이다.

개선 후보:

- 비회원 화면을 더 서비스 소개답게 정리
- 회원 화면에 관심 피드, 최근 알림, 추적 중 항목 레이아웃을 더 구체화
- feed 서비스가 스트리머 활동 알림과 가격 알림을 합친 서비스라는 점을 더 명확하게 표현
- feed 하단 툴바 항목 확정

## 서비스별 하단 툴바 정책 정리

현재 하단 툴바 후보:

blog:

- 피드
- 인기
- 작성
- 홈

feed:

- 피드
- 관심
- blog
- 홈

남은 고민:

- 서비스별로 다른 하단 툴바를 유지할지
- 플랫폼 홈으로 돌아가는 항목은 항상 둘지
- 같은 위치에 같은 역할을 배치할지
- 검색과 작성 같은 액션을 하단에 둘지, 상단 액션 또는 별도 화면으로 분리할지

## 서비스별 홈 로고와 상단 서비스명 디자인

상단바에는 `choimory.dev / blog`, `choimory.dev / feed`처럼 서비스 맥락이 표시된다.

개선 후보:

- `/ blog`, `/ feed` 같은 서비스명에 색상 또는 스타일 추가
- 서비스별 홈 로고 느낌을 조금 더 명확하게 만들기
- 다만 320px 모바일에서도 줄바꿈과 표시 안정성을 유지해야 한다.

---

# 추천 step2

추천 step2는 다음 조합이다.

1. feed 화면 디테일 개선
2. 서비스별 하단 툴바 정책 정리

이유:

- `/feed`는 이제 라우트와 구조는 생겼지만 아직 안내/뼈대 느낌이 강하다.
- feed를 먼저 다듬으면 플랫폼, 블로그, 피드 3축이 더 선명해진다.
- 하단 툴바 정책을 함께 정리하면 blog와 feed가 각각 따로 노는 느낌을 줄일 수 있다.

다음 구현 방향:

- feed 비회원 화면을 서비스 소개형으로 고도화
- feed 회원 화면의 관심 피드/최근 알림/추적 중 항목을 더 구체화
- blog/feed 하단 툴바 항목과 역할을 비교해 정리
- 필요한 경우 상단 서비스명(`/ blog`, `/ feed`)의 스타일 보강은 이후 단계로 분리

---

# step2 구현 전 분석

## 현재 상태

- 플랫폼 홈은 `blog/feed` 활성, `memo/schedule/account-book` 준비 중으로 정리되었다.
- `/feed` 라우트와 `features/feed` 구조는 생겼다.
- feed는 guest/member 분기 가능하다.
- 하지만 feed 화면은 아직 소개와 목록 뼈대 수준이다.
- blog와 feed 하단 툴바는 각각 따로 정해져 있어서 정책이 덜 정리되어 있다.

## 문제점

1. feed 비회원 화면이 아직 서비스의 매력을 충분히 설명하지 못한다.
   - 스트리머 활동 알림과 가격 알림이 한 서비스 안에 들어간다는 컨셉은 있지만, 화면에서 강하게 보이지 않는다.
   - 왜 로그인해야 하는지가 조금 약하다.
2. feed 회원 화면이 아직 실제 사용 화면처럼 보이지 않는다.
   - 요약 카드와 최근 알림은 있지만, 내가 뭘 추적 중인지가 약하다.
   - 관심 피드, 추적 중 항목, 최근 이벤트가 구분되면 더 서비스답게 보인다.
3. 하단 툴바 정책이 서비스별로 일관되지 않다.
   - blog: `피드 / 인기 / 작성 / 홈`
   - feed: `피드 / 관심 / blog / 홈`
   - `blog` 항목이 feed 하단에 들어가는 건 살짝 어색하다.
   - 플랫폼형 앱이면 각 서비스 하단에서 `홈`은 플랫폼 홈으로, 나머지는 서비스 내부 기능으로 두는 편이 좋다.

## 추천 하단 툴바 정책

플랫폼 홈:

- 홈
- 서비스
- 알림
- 내 공간

blog:

- 피드
- 인기
- 작성
- 홈

feed:

- 피드
- 추적
- 알림
- 홈

의미:

- 첫 번째: 현재 서비스 메인
- 두 번째: 서비스 핵심 분류/탭
- 세 번째: 주요 액션 또는 알림/작성
- 네 번째: 플랫폼 홈

## feed 화면 개선 방향

비회원:

- 상단 소개 카드 강화
  - 방송 시작과 가격 하락을 한 피드에서 확인한다는 메시지
  - 로그인/회원가입 CTA
- 알림 유형 카드
  - 스트리머 활동
  - 가격 하락
  - 사용자 정의 조건
- 로그인하면 가능한 것 섹션
  - 관심 대상 저장
  - 목표 가격 설정
  - 알림 히스토리 확인

회원:

- 요약 카드 유지
  - 추적 중
  - 새 알림
  - 관심 항목
- 추적 중인 항목 섹션 추가
  - 스트리머 샘플
  - 상품 샘플
- 최근 이벤트 섹션 정리
  - 방송 시작
  - 가격 하락
  - 새 콘텐츠
- 나중에 실제 API를 붙이기 쉽게 ViewModel에 데이터를 분리한다.

## 구현 범위

- `FeedViewModel` 확장
  - `trackingItems`
  - `guestBenefits`
  - `signals`
  - `activities`
- `FeedView` UI 개선
  - guest/member 분기 내부 구조 개선
  - 회원용 추적 중 항목 섹션 추가
  - 비회원용 로그인 유도 섹션 보강
- feed 하단 툴바 변경
  - `피드 / 추적 / 알림 / 홈`
- blog 하단 툴바는 이번에는 유지한다.
  - blog는 별도 UX 확정 작업으로 미룬다.
  - 단, 정책 문서에는 blog/feed 정책을 같이 기록한다.

## 이번 작업에서 제외

- 실제 추적 추가 기능
- 실제 알림 API
- 실제 가격 데이터
- 실제 스트리머 API
- 하단 툴바 클릭 동작 구현
- blog 하단 툴바 변경
- 검색 화면 구현

## 추천 구현 순서

1. `feedTypes.ts`에 `FeedTrackingItem`, `FeedGuestBenefit` 추가
2. `feedViewModel` 샘플 데이터 확장
3. `FeedView`에서 guest/member 섹션 구조 개선
4. feed 하단 툴바를 `피드 / 추적 / 알림 / 홈`으로 변경
5. 구현 후 `task2.md` 작성
6. `tsc`, `lint`, `build` 실행

---

# 추가사항 분석

253번줄 추가사항 기준으로 보면, 이번 추가사항은 단순 문구 수정이 아니라 상단 전역 액션 UX, 서비스별 네비게이션 정책, 플랫폼 회원 홈 구조를 다시 잡는 작업이다.

## 범위 분리

즉시 반영 가능한 것:

- 플랫폼 비로그인 랜딩에서 `blog`, `feed` 같은 영어를 한글 `블로그`, `피드`로 변경
- 플랫폼, 블로그, 피드 하단 툴바 항목을 새 정책으로 변경
- 필요한 아이콘 추가
  - 공지사항
  - 인기용 트로피
  - 추적용 망원경
  - 내 블로그/내 피드용 사용자 또는 북마크 계열 아이콘 재사용 가능

구조 작업이 필요한 것:

- 상단 검색 버튼 클릭 시 중앙 검색 모달
- 상단 알림 버튼 클릭 시 알림 팝오버
- `AppTopBar`는 현재 단순 버튼만 렌더링하므로 client component 전환 또는 내부 client subcomponent가 필요함
- 검색/알림 UI는 모든 서비스에서 공통으로 쓰일 것이므로 `shared/ui` 쪽 설계가 적합함

별도 화면 설계가 필요한 것:

- 플랫폼 로그인 메인에서 서비스별 내 활동 기록 카드 구성
- 이 작업은 `MemberServiceFeedSection`을 확장하는 작업
- 서비스별 활동 카드 구조를 어떻게 잡을지 먼저 정해야 함

## 하단 툴바 정책 분석

새 정책:

플랫폼:

- 홈
- 공지사항
- 서비스
- 내 정보

블로그:

- 홈
- 인기
- 팔로잉
- 내 블로그

피드:

- 홈
- 인기
- 추적
- 내 피드

좋은 점:

- 각 서비스마다 4개 항목으로 유지되어 모바일 하단바에 적당하다.
- 첫 번째 `홈`을 각 서비스 홈으로 해석할 수 있다.
- 마지막 항목을 내 공간 또는 내 서비스 홈으로 둬서 개인 영역 진입이 명확하다.

주의할 점:

- 플랫폼의 `홈`은 `/`
- 블로그의 `홈`은 `/blog`
- 피드의 `홈`은 `/feed`
- 플랫폼으로 돌아가는 이동은 상단 로고 `choimory.dev`가 담당한다.
- 따라서 하단 툴바의 `홈`은 현재 영역의 홈으로 통일하는 것이 좋다.

## 네이밍 제안

블로그의 `팔로잉`은 그대로 사용해도 좋다.

대안:

- 팔로잉
- 구독
- 관계
- 소식
- 내 구독

인스타그램형 블로그/SNS라면 `팔로잉`이 가장 직관적이다.

피드의 `추적`도 적절하다.

망원경 아이콘까지 함께 사용하면 의미가 더 잘 살아난다.

## 상단 검색 UX 분석

요구사항:

- 상단 검색 버튼 클릭
- 화면 배경 흐림
- 중앙 검색창 표시
- 왼쪽 selectbox로 검색 대상 선택
- 오른쪽 검색 버튼

추천 구조:

- `AppTopBar` 전체를 client component로 바꾸는 방법이 가장 단순하다.
- 하지만 더 깔끔하게 하려면 검색/알림 버튼 영역만 클라이언트 컴포넌트로 분리한다.
- 추천은 `TopBarActions` 분리다.
  - 상단바 전체를 client로 만들지 않아도 된다.
  - 검색 모달/알림 팝오버 상태를 액션 영역 안에서 관리할 수 있다.

예상 구조:

```text
src/shared/ui/TopBarActions.tsx
src/shared/ui/SearchOverlay.tsx
src/shared/ui/NotificationPopover.tsx
```

초기에는 실제 검색/알림 기능 없이 UI만 구현한다.

검색 대상:

- 전체
- 블로그
- 피드

알림 샘플:

- 새 댓글
- 방송 시작
- 가격 하락

## 상단 알림 UX 분석

요구사항:

- 모든 서비스 알림
- 클릭 시 알림 버튼 위에 작은 창
- 탭: 모든 알림 / 서비스별 알림
- 알림 내역 표시

주의할 점:

- 모바일 320px에서는 버튼 위 작은 창이 화면 밖으로 나갈 수 있다.
- 따라서 데스크톱/태블릿에서는 버튼 근처 팝오버, 모바일에서는 화면 중앙 또는 하단 sheet에 가까운 형태가 안정적이다.
- 초기 구현은 `absolute right-0 top-12 w-[min(calc(100vw-2rem),320px)]` 형태로 잡으면 안정적이다.

## 플랫폼 로그인 메인 카드화

현재 회원용 플랫폼 홈:

- 오늘 요약 3개
- 최근 활동 리스트

요구사항:

- 서비스를 카드별로 구성
- 서비스별 내 활동 기록 표시

추천 구조:

- `MemberServiceFeedSection`을 확장한다.
- 서비스 카드 예:
  - 블로그: 새 댓글, 인기 글, 팔로잉 소식
  - 피드: 새 알림, 추적 중 항목, 가격 하락
  - 메모/일정/가계부: 준비 중 카드 또는 비활성 카드

다만 이 작업은 상단 검색/알림보다 범위가 커질 수 있으므로 단계를 나누는 것이 좋다.

## 추가사항 추천 구현 순서

1. 문구와 하단 툴바 정책 먼저 반영
   - 플랫폼 비로그인 랜딩 영어를 한글로 변경
   - 플랫폼 툴바: 홈 / 공지사항 / 서비스 / 내 정보
   - 블로그 툴바: 홈 / 인기 / 팔로잉 / 내 블로그
   - 피드 툴바: 홈 / 인기 / 추적 / 내 피드
   - 필요한 아이콘 추가: `trophy`, `telescope`, `megaphone`
2. 상단 검색/알림 UI 추가
   - `TopBarActions` 클라이언트 컴포넌트 추가
   - 검색 오버레이 추가
   - 알림 팝오버 추가
   - 실제 기능 없이 UI 상태만 구현
3. 플랫폼 회원 메인 카드화
   - `MemberServiceFeedSection` 확장
   - 서비스별 활동 카드 추가
   - blog/feed active, 미래 서비스 planned 구분 반영

## 추가사항 구현 제외 범위

- 실제 검색 API
- 실제 알림 API
- 검색 결과 화면
- 알림 읽음 처리
- 팔로잉 실제 데이터
- 내 블로그 실제 라우트
- 내 피드 실제 라우트
- 공지사항 실제 라우트
- 서비스 목록 실제 라우트

## 추가사항 결론

이번 추가사항은 한 번에 다 하면 범위가 크다.

추천 1차:

- 하단 툴바 정책 변경
- 필요한 아이콘 추가
- 비로그인 랜딩 문구 한글화

추천 2차:

- 상단 검색/알림 UI

추천 3차:

- 플랫폼 회원 메인 서비스별 카드화

화면 체감이 큰 작업은 상단 검색/알림이므로, 일정에 여유가 있다면 1차와 2차를 한 번에 진행해도 괜찮다.

---

# 추가사항

- 플랫폼 비로그인 랜딩에서 소개문구에 blog, feed 같은 영어를 한글로 변경
- 플랫폼 로그인 메인에서는 서비스를 카드별로 구성해서, 서비스별 내 활동 기록들을 표시
- 상단 검색버튼: 통합검색, 서비스별 검색을 지원 (누르면 화면 백그라운드로 흐려지면서 화면 중앙에 검색창이 뜨고, 검색창은 검색바를 하나 놓고 검색바 왼쪽에 selectbox로 서비스를 선택, 검색바 우측엔 검색버튼)
- 상단 알림버튼: 모든 서비스의 알림 (누르면 모든 알림이 조그만 창으로 알림버튼 위에 뜨고, 창 내용은 위에 탭으로 모든 알림/서비스별 알림 있고 그 밑으로 알림 내역들.)
- 플랫폼 툴바: 홈, 공지사항, 서비스, 내 정보
- 블로그 툴바: 홈, 인기(트로피 아이콘), 팔로잉(팔로우한 사람들의 활동만 나오는 피드인데 네이밍을 못정하겠음), 내 블로그 
- 피드 툴바: 홈, 인기, 추적(망원경 아이콘), 내 피드
